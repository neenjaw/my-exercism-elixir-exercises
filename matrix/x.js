function fizzbuzz(triggers) {
  for (let i = 1; i <= 100; i++) {
    // const sound = triggers.reduce((accResult, [divisor, note]) => {
    //   return i % divisor === 0 ? accResult + note : accResult
    // }, '')
    console.log('A')
  }
}
